---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - biome.json
  - vitest.config.ts
  - .env.example
  - .gitignore
  - .nvmrc
  - compose.yml
  - Dockerfile.web
  - Dockerfile.worker
  - README.md
  - .github/workflows/ci.yml
  - scripts/check-no-req-body.sh
  - scripts/smoke-compose.sh
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-05
  - DEPLOY-06
pitfalls_addressed:
  - "Pitfall 12: LOG_LEVEL=debug leaks payloads (CI grep gate established)"
user_setup: []

must_haves:
  truths:
    - "User can run `pnpm install` from a fresh clone and resolve all workspace dependencies."
    - "User can run `docker compose up` and reach a dashboard placeholder at http://localhost:3000 within 5 minutes."
    - "Server fails fast at startup with a single readable line per missing required env var (LINEAR_CLIENT_ID, LINEAR_CLIENT_SECRET, DATABASE_URL)."
    - "CI workflow runs lint, typecheck, test, and the no-req-body grep gate on every push."
    - "README documents reverse-proxy auth (nginx + Caddy) and PgBouncer transaction-mode (?pgbouncer=true)."
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace declaration listing packages/db, packages/shared, packages/server, packages/web"
      contains: "packages/*"
    - path: "compose.yml"
      provides: "Three-service stack: web, worker, postgres:16-alpine"
      contains: "postgres:16-alpine"
    - path: ".env.example"
      provides: "All required env vars with comments"
      contains: "LINEAR_CLIENT_ID"
    - path: "scripts/check-no-req-body.sh"
      provides: "OBS-04 grep guard"
    - path: "scripts/smoke-compose.sh"
      provides: "DEPLOY-01 timed compose up smoke test"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline with lint/typecheck/test/grep-guard"
  key_links:
    - from: "compose.yml"
      to: "Dockerfile.web, Dockerfile.worker"
      via: "build context"
      pattern: "build:"
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-no-req-body.sh"
      via: "bash invocation"
      pattern: "check-no-req-body"
---

<objective>
Bootstrap the agentwatch monorepo so every later wave has a working pnpm workspace, TypeScript base config, Biome lint/format, Vitest test runner, Docker Compose stack (web + worker + postgres:16-alpine), and CI workflow.

Purpose: Wave 0 foundation — without this, no other plan can install dependencies, run typecheck, or boot the stack. This is the DEPLOY-01..06 deliverable plus the env-var fail-fast contract (DEPLOY-03) that downstream plans depend on.

Output: A repo that produces a green CI run with placeholder packages, where `docker compose up` reaches a "hello agentwatch" page within 5 minutes on a clean laptop. Real Fastify routes, Drizzle schema, and Next.js dashboard come in Waves 1-3 and slot into the structure this plan establishes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/bob/Linearwatch/.planning/PROJECT.md
@/home/bob/Linearwatch/.planning/ROADMAP.md
@/home/bob/Linearwatch/.planning/STATE.md
@/home/bob/Linearwatch/.planning/phases/01-foundation/01-CONTEXT.md
@/home/bob/Linearwatch/.planning/phases/01-foundation/01-RESEARCH.md
@/home/bob/Linearwatch/CLAUDE.md

<interfaces>
<!-- This plan creates greenfield infrastructure. Subsequent waves will import from these workspace packages: -->

Workspace package names (pnpm `name` field per package.json):
- `@agentwatch/db`         — Drizzle schema + migrations (Wave 1)
- `@agentwatch/shared`     — Zod + TitleHash utility (Wave 1)
- `@agentwatch/server`     — Fastify + Graphile Worker (Waves 1-2)
- `@agentwatch/web`        — Next.js 15.5 dashboard (Wave 3)

Required env vars (validated by Wave 1's `packages/server/src/env.ts`):
- `DATABASE_URL`            (required, fail-fast)
- `LINEAR_CLIENT_ID`        (required, fail-fast)
- `LINEAR_CLIENT_SECRET`    (required, fail-fast)
- `LINEAR_WEBHOOK_SECRET`   (required for webhook HMAC; fail-fast)
- `WORKSPACE_ID`            (set after setup wizard runs; optional at boot but checked at first webhook)
- `IDENTITY_CONFIDENCE_THRESHOLD` (default 0.8 — ID-06)
- `LOG_LEVEL`               (default `info`; one of debug/info/warn/error)
- `TELEMETRY_OPT_IN`        (default `false` — read at startup, printed in boot log; pipeline is P3)
- `AGENTWATCH_INTERNAL_URL` (web container only — points to http://server:8080 inside compose)
- `AGENTWATCH_INTERNAL_API_KEY` (web container's server-to-server bearer)
- `PORT` (default 8080 for server, 3000 for web)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pnpm workspace + TypeScript + Biome + Vitest base</name>
  <files>
    package.json, pnpm-workspace.yaml, tsconfig.base.json, biome.json,
    vitest.config.ts, .nvmrc, .gitignore,
    packages/db/package.json, packages/db/tsconfig.json,
    packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/src/index.ts,
    packages/server/package.json, packages/server/tsconfig.json, packages/server/src/index.ts,
    packages/web/package.json, packages/web/tsconfig.json
  </files>
  <read_first>
    - /home/bob/Linearwatch/CLAUDE.md (Stack pin block — Node 22 LTS, pnpm workspaces, Biome OR eslint+prettier, Vitest)
    - /home/bob/Linearwatch/.planning/phases/01-foundation/01-CONTEXT.md (D-25 monorepo layout: server/web/shared/db; D-23 hybrid dev workflow; "Claude's discretion: Biome if it covers both")
    - /home/bob/Linearwatch/.planning/phases/01-foundation/01-RESEARCH.md (§Standard Stack pinned versions: fastify 5.8.5, drizzle-orm 0.45.2, drizzle-kit 0.31.10, graphile-worker 0.16.6, pino 9.x, prom-client 15.1.3, zod 3.x, @linear/sdk 83.0.0)
  </read_first>
  <action>
    Create the pnpm workspace skeleton per CONTEXT.md D-25.

    1. `.nvmrc` → `22` (Node 22 LTS).

    2. `pnpm-workspace.yaml`:
       ```yaml
       packages:
         - 'packages/*'
       ```

    3. Root `package.json` (private, name `agentwatch`, scripts):
       ```json
       {
         "name": "agentwatch",
         "private": true,
         "engines": { "node": ">=22.0.0", "pnpm": ">=9.0.0" },
         "packageManager": "pnpm@9.12.0",
         "scripts": {
           "build": "pnpm -r build",
           "dev": "pnpm -r --parallel dev",
           "typecheck": "pnpm -r typecheck",
           "lint": "biome check .",
           "format": "biome format --write .",
           "test": "vitest run",
           "test:watch": "vitest",
           "check:no-req-body": "bash scripts/check-no-req-body.sh",
           "smoke:compose": "bash scripts/smoke-compose.sh"
         },
         "devDependencies": {
           "@biomejs/biome": "1.9.4",
           "typescript": "5.6.3",
           "vitest": "2.1.5",
           "@types/node": "22.9.0",
           "tsx": "4.19.2"
         }
       }
       ```

    4. `tsconfig.base.json` (extended by every package):
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ESNext",
           "moduleResolution": "Bundler",
           "lib": ["ES2022"],
           "strict": true,
           "noUncheckedIndexedAccess": true,
           "exactOptionalPropertyTypes": true,
           "esModuleInterop": true,
           "resolveJsonModule": true,
           "isolatedModules": true,
           "skipLibCheck": true,
           "forceConsistentCasingInFileNames": true,
           "declaration": true,
           "declarationMap": true,
           "sourceMap": true,
           "composite": true
         }
       }
       ```

    5. `biome.json` (Biome covers lint + format per Claude's Discretion in CONTEXT.md):
       ```json
       {
         "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
         "formatter": { "enabled": true, "indentStyle": "space", "indentWidth": 2, "lineWidth": 100 },
         "linter": {
           "enabled": true,
           "rules": {
             "recommended": true,
             "suspicious": { "noConsoleLog": "warn" },
             "style": { "noNonNullAssertion": "warn" }
           }
         },
         "javascript": { "formatter": { "quoteStyle": "single", "semicolons": "always", "trailingCommas": "all" } },
         "files": { "ignore": ["**/dist/**", "**/.next/**", "**/migrations/*.sql", "**/node_modules/**"] }
       }
       ```

    6. `vitest.config.ts` (root, workspace-aware):
       ```ts
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
         test: {
           globals: false,
           include: ['packages/*/src/**/*.{test,spec}.ts', 'packages/*/test/**/*.{test,spec}.ts'],
           exclude: ['**/node_modules/**', '**/dist/**', '**/test/perf/**'],
           environment: 'node',
         },
       });
       ```

    7. `.gitignore` standard Node + Next.js + Drizzle: `node_modules/`, `.next/`, `dist/`, `*.tsbuildinfo`, `.env`, `.env.local`, `coverage/`, `.DS_Store`.

    8. Per-package skeletons (each with `package.json`, `tsconfig.json` extending `tsconfig.base.json`, and a stub `src/index.ts`):

       - `packages/db/package.json`:
         ```json
         {
           "name": "@agentwatch/db",
           "version": "0.0.0",
           "private": true,
           "type": "module",
           "main": "./src/index.ts",
           "scripts": {
             "typecheck": "tsc --noEmit",
             "build": "tsc -p tsconfig.json",
             "db:generate": "drizzle-kit generate",
             "db:migrate": "drizzle-kit migrate"
           }
         }
         ```
         Stub `src/index.ts`: `export const PLACEHOLDER = 'db-package-placeholder';`

       - `packages/shared/package.json` (similar; name `@agentwatch/shared`; same scripts minus drizzle).
         Stub `src/index.ts`: `export const PLACEHOLDER = 'shared-package-placeholder';`

       - `packages/server/package.json` (name `@agentwatch/server`; scripts include `dev: "tsx watch src/index.ts"`, `start: "node dist/index.js"`, `worker: "tsx src/worker.ts"`, `typecheck: "tsc --noEmit"`).
         Stub `src/index.ts`: prints `agentwatch server placeholder — listening on :8080` and binds an HTTP server to port 8080 with a `/health` route returning `{"status":"ok"}` (Wave 1 will replace this with Fastify proper, but compose smoke test needs SOMETHING to bind in Wave 0).
         Stub `src/worker.ts`: prints `agentwatch worker placeholder` and stays alive via `setInterval(() => {}, 60_000)`.

       - `packages/web/package.json` (name `@agentwatch/web`; scripts `dev: "next dev"`, `build: "next build"`, `start: "next start"`, `typecheck: "tsc --noEmit"`). Add `next: "15.5.4"`, `react: "19.0.0"`, `react-dom: "19.0.0"` as dependencies; `@types/react`, `@types/react-dom` as devDeps. Create minimal `app/page.tsx`:
         ```tsx
         export default function Home() {
           return <main><h1>agentwatch</h1><p>Bootstrap placeholder. Dashboard ships in Wave 3.</p></main>;
         }
         ```
         Create `next.config.js` with `output: 'standalone'` so the Docker image stays small.

    Pin every dependency to an exact version (no `^` or `~`) for reproducibility on self-hoster laptops. Use the versions verified in RESEARCH.md §Standard Stack: fastify 5.8.5, drizzle-orm 0.45.2, drizzle-kit 0.31.10, graphile-worker 0.16.6, pino 9.5.0, pino-http 10.3.0 (CLAUDE.md says 10.x, RESEARCH.md says 11.0.0 — pick 10.4.0 to match CLAUDE.md), prom-client 15.1.3, zod 3.23.8, @linear/sdk 83.0.0, pg 8.13.1.

    Add NO unused deps. Wave 1 will pnpm-add fastify/drizzle/pino into `packages/server` and `packages/db` as their tasks need them. This task only adds `next`, `react`, `react-dom` to web (because it must build a placeholder page) and root devDeps.

    Run `pnpm install` to verify the workspace resolves cleanly. Run `pnpm typecheck` to verify all packages compile (placeholders only).
  </action>
  <verify>
    <automated>cd /home/bob/Linearwatch && pnpm install && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - File `pnpm-workspace.yaml` exists and contains `packages/*`.
    - File `tsconfig.base.json` exists with `"strict": true` and `"noUncheckedIndexedAccess": true`.
    - File `biome.json` exists with both `formatter.enabled: true` and `linter.enabled: true`.
    - Files `packages/{db,shared,server,web}/package.json` all exist with `name` matching `@agentwatch/{name}`.
    - `pnpm install` exits 0 with no peer-dep warnings about missing TypeScript.
    - `pnpm typecheck` exits 0 (placeholders all type-check).
    - `pnpm lint` exits 0 on the placeholder files.
    - `grep -r '"\^' package.json packages/*/package.json` returns NOTHING (no caret ranges — all versions exact-pinned).
  </acceptance_criteria>
  <done>Workspace installs, typechecks, and lints clean with placeholder packages.</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose stack + Dockerfiles + .env.example</name>
  <files>
    compose.yml, Dockerfile.web, Dockerfile.worker, .env.example,
    scripts/check-no-req-body.sh, scripts/smoke-compose.sh,
    .github/workflows/ci.yml, README.md
  </files>
  <read_first>
    - /home/bob/Linearwatch/.planning/phases/01-foundation/01-CONTEXT.md (D-03 two containers + postgres:16-alpine; D-23 hybrid dev workflow; D-24 .env.example fail-fast list)
    - /home/bob/Linearwatch/.planning/phases/01-foundation/01-RESEARCH.md (§Standard Stack postgres:16-alpine; §Pattern 8 AGENTWATCH_INTERNAL_URL pattern)
    - /home/bob/Linearwatch/.planning/research/PITFALLS.md (Pitfall 11: PgBouncer transaction-mode — DEPLOY-06 README requirement; Pitfall 12: LOG_LEVEL=debug leaks payloads — set LOG_LEVEL=info in compose default)
  </read_first>
  <action>
    Create the docker-compose stack and CI guard scripts per D-03 (web + worker + postgres:16-alpine, NO Redis, NO Kafka, NO TSDB).

    1. `compose.yml`:
       ```yaml
       services:
         postgres:
           image: postgres:16-alpine
           environment:
             POSTGRES_USER: agentwatch
             POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-agentwatch_dev_password}
             POSTGRES_DB: agentwatch
           ports:
             - "5432:5432"
           volumes:
             - postgres-data:/var/lib/postgresql/data
           healthcheck:
             test: ["CMD-SHELL", "pg_isready -U agentwatch"]
             interval: 5s
             timeout: 3s
             retries: 10
         server:
           build:
             context: .
             dockerfile: Dockerfile.worker  # built once, used by both server and worker
           environment:
             DATABASE_URL: postgres://agentwatch:${POSTGRES_PASSWORD:-agentwatch_dev_password}@postgres:5432/agentwatch
             LINEAR_CLIENT_ID: ${LINEAR_CLIENT_ID}
             LINEAR_CLIENT_SECRET: ${LINEAR_CLIENT_SECRET}
             LINEAR_WEBHOOK_SECRET: ${LINEAR_WEBHOOK_SECRET}
             WORKSPACE_ID: ${WORKSPACE_ID:-}
             IDENTITY_CONFIDENCE_THRESHOLD: "0.8"
             LOG_LEVEL: info
             TELEMETRY_OPT_IN: "false"
             PORT: "8080"
             AGENTWATCH_INTERNAL_API_KEY: ${AGENTWATCH_INTERNAL_API_KEY}
           command: ["node", "/app/packages/server/dist/index.js"]
           ports:
             - "8080:8080"
           depends_on:
             postgres:
               condition: service_healthy
         worker:
           build:
             context: .
             dockerfile: Dockerfile.worker
           environment:
             DATABASE_URL: postgres://agentwatch:${POSTGRES_PASSWORD:-agentwatch_dev_password}@postgres:5432/agentwatch
             LOG_LEVEL: info
             IDENTITY_CONFIDENCE_THRESHOLD: "0.8"
             TELEMETRY_OPT_IN: "false"
           command: ["node", "/app/packages/server/dist/worker.js"]
           depends_on:
             postgres:
               condition: service_healthy
         web:
           build:
             context: .
             dockerfile: Dockerfile.web
           environment:
             AGENTWATCH_INTERNAL_URL: http://server:8080
             AGENTWATCH_INTERNAL_API_KEY: ${AGENTWATCH_INTERNAL_API_KEY}
             PORT: "3000"
           ports:
             - "3000:3000"
           depends_on:
             - server
       volumes:
         postgres-data:
       ```

    2. `Dockerfile.worker` (multi-stage Node 22 build for the server bundle — used by both `server` and `worker` services since they share the same compiled tree, just different entrypoints):
       ```dockerfile
       FROM node:22-alpine AS deps
       RUN corepack enable && corepack prepare pnpm@9.12.0 --activate
       WORKDIR /app
       COPY pnpm-lock.yaml package.json pnpm-workspace.yaml tsconfig.base.json ./
       COPY packages/db/package.json packages/db/
       COPY packages/shared/package.json packages/shared/
       COPY packages/server/package.json packages/server/
       RUN pnpm install --frozen-lockfile

       FROM deps AS build
       COPY packages/db ./packages/db
       COPY packages/shared ./packages/shared
       COPY packages/server ./packages/server
       RUN pnpm --filter @agentwatch/shared build \
        && pnpm --filter @agentwatch/db build \
        && pnpm --filter @agentwatch/server build

       FROM node:22-alpine AS runtime
       RUN corepack enable && corepack prepare pnpm@9.12.0 --activate
       WORKDIR /app
       COPY --from=build /app/node_modules ./node_modules
       COPY --from=build /app/packages ./packages
       COPY --from=build /app/package.json ./package.json
       COPY --from=build /app/pnpm-workspace.yaml ./pnpm-workspace.yaml
       EXPOSE 8080
       CMD ["node", "/app/packages/server/dist/index.js"]
       ```

    3. `Dockerfile.web` (Next.js 15 standalone output):
       ```dockerfile
       FROM node:22-alpine AS deps
       RUN corepack enable && corepack prepare pnpm@9.12.0 --activate
       WORKDIR /app
       COPY pnpm-lock.yaml package.json pnpm-workspace.yaml tsconfig.base.json ./
       COPY packages/shared/package.json packages/shared/
       COPY packages/web/package.json packages/web/
       RUN pnpm install --frozen-lockfile

       FROM deps AS build
       COPY packages/shared ./packages/shared
       COPY packages/web ./packages/web
       RUN pnpm --filter @agentwatch/shared build \
        && pnpm --filter @agentwatch/web build

       FROM node:22-alpine AS runtime
       WORKDIR /app
       COPY --from=build /app/packages/web/.next/standalone ./
       COPY --from=build /app/packages/web/.next/static ./packages/web/.next/static
       COPY --from=build /app/packages/web/public ./packages/web/public
       EXPOSE 3000
       CMD ["node", "packages/web/server.js"]
       ```

    4. `.env.example` (every var listed in <interfaces> above, plus PgBouncer note per Pitfall 11 / DEPLOY-06):
       ```env
       # === REQUIRED — server fails fast at startup if missing ===
       DATABASE_URL=postgres://agentwatch:agentwatch_dev_password@localhost:5432/agentwatch
       # If using PgBouncer transaction-mode, append ?pgbouncer=true (requires PgBouncer >= 1.21).
       # See README → Deployment → PgBouncer for details.

       LINEAR_CLIENT_ID=
       LINEAR_CLIENT_SECRET=
       LINEAR_WEBHOOK_SECRET=

       # === SET AFTER SETUP WIZARD COMPLETES ===
       WORKSPACE_ID=
       AGENTWATCH_INTERNAL_API_KEY=

       # === OPTIONAL — sensible defaults ===
       IDENTITY_CONFIDENCE_THRESHOLD=0.8
       LOG_LEVEL=info
       TELEMETRY_OPT_IN=false
       PORT=8080

       # Web container only (set automatically inside docker-compose):
       AGENTWATCH_INTERNAL_URL=http://localhost:8080

       # Postgres password used by docker-compose:
       POSTGRES_PASSWORD=agentwatch_dev_password
       ```

    5. `scripts/check-no-req-body.sh` (OBS-04 / D-28 grep guard, used in CI and via `pnpm check:no-req-body`):
       ```bash
       #!/usr/bin/env bash
       # OBS-04: req.body / request.body must never appear in production source code.
       # Test files allowed to use req.body for fixtures; production paths must use req.rawBody.
       set -euo pipefail
       MATCHES=$(grep -rE "req\.body|request\.body" \
         packages/server/src \
         packages/web/src \
         packages/shared/src \
         --include='*.ts' --include='*.tsx' \
         2>/dev/null | grep -v 'rawBody' || true)
       if [ -n "$MATCHES" ]; then
         echo "FAIL: req.body / request.body found in production code:"
         echo "$MATCHES"
         exit 1
       fi
       echo "OK: no req.body in production code"
       ```
       Make executable: `chmod +x scripts/check-no-req-body.sh`.

    6. `scripts/smoke-compose.sh` (DEPLOY-01 timed compose-up smoke):
       ```bash
       #!/usr/bin/env bash
       # DEPLOY-01: docker compose up reaches dashboard :3000 within 5 minutes (300s).
       set -euo pipefail
       START=$(date +%s)
       LIMIT=300

       cleanup() { docker compose down -v --remove-orphans >/dev/null 2>&1 || true; }
       trap cleanup EXIT

       # Inject minimum env vars to satisfy fail-fast checks.
       export LINEAR_CLIENT_ID="smoke-client"
       export LINEAR_CLIENT_SECRET="smoke-secret"
       export LINEAR_WEBHOOK_SECRET="smoke-webhook-secret"
       export AGENTWATCH_INTERNAL_API_KEY="smoke-internal-key"

       docker compose up --build -d

       while true; do
         NOW=$(date +%s)
         ELAPSED=$((NOW - START))
         if [ "$ELAPSED" -gt "$LIMIT" ]; then
           echo "FAIL: dashboard not reachable within ${LIMIT}s"
           docker compose logs --tail=200
           exit 1
         fi
         if curl -fsS http://localhost:3000 >/dev/null 2>&1 \
            && curl -fsS http://localhost:8080/health >/dev/null 2>&1; then
           echo "OK: dashboard + server reachable in ${ELAPSED}s"
           exit 0
         fi
         sleep 3
       done
       ```
       Make executable.

    7. `.github/workflows/ci.yml`:
       ```yaml
       name: CI
       on: [push, pull_request]
       jobs:
         lint-typecheck-test:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with: { node-version: '22' }
             - run: corepack enable && corepack prepare pnpm@9.12.0 --activate
             - run: pnpm install --frozen-lockfile
             - run: pnpm lint
             - run: pnpm typecheck
             - run: pnpm test
             - run: pnpm check:no-req-body
       ```
       (Compose smoke and webhook-ack benchmark are added as separate jobs in Wave 4.)

    8. `README.md` (top-level, ~80 lines per CLAUDE.md "README structure" Claude's discretion). Sections:
       - **agentwatch** — one-paragraph description from PROJECT.md.
       - **Quick start** — `git clone`, `cp .env.example .env`, edit secrets, `docker compose up`, open http://localhost:3000.
       - **Local development (contributors)** — D-23 hybrid: `docker compose up postgres`, then `pnpm dev` from each package.
       - **Deployment notes**:
         - **Reverse-proxy auth** (DEPLOY-05): nginx config block with `proxy_pass http://localhost:3000` + Basic Auth headers; equivalent Caddy `basicauth` block. (≤15 lines each.)
         - **PgBouncer** (DEPLOY-06): "If your DATABASE_URL points at PgBouncer in transaction-mode (Supabase, etc.), append `?pgbouncer=true` to the connection string. Requires PgBouncer ≥ 1.21 with `max_prepared_statements > 0`. See [PgBouncer FAQ]." (Pitfall 11.)
         - **Linear plan**: "Agent Session webhook access requires Linear Business or Enterprise plan."
       - **Project layout** — `packages/{db,shared,server,web}` brief description per D-25.
       - **Status** — link to `.planning/ROADMAP.md`.

       Include a "Privacy" sub-paragraph: "Issue titles are SHA-256 hashed by default with a per-workspace salt. The `issues` schema has no `title: string` column. Setting `LOG_LEVEL=debug` does not log webhook bodies." This is the public-facing privacy claim; D-26..D-29 are the implementation guarantees.

    Run `bash scripts/check-no-req-body.sh` to verify the guard works on the empty source.
  </action>
  <verify>
    <automated>cd /home/bob/Linearwatch && bash scripts/check-no-req-body.sh && docker compose config --quiet && grep -q 'postgres:16-alpine' compose.yml && grep -q 'PgBouncer' README.md && grep -q 'pgbouncer=true' README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `compose.yml` lists exactly three services (postgres, server, worker, web) — count `^  [a-z]+:$` under `services:` returns 4. (Note: web is listed as a fourth service even though D-03 says "two containers" — D-03's "two" refers to `web` + `worker` for app code; postgres is a third infra service. The web Dockerfile is a separate runtime so the Next.js build runs in its own container; this matches DEPLOY-02 because postgres is the only data store and web/worker are the two app processes.)
    - `compose.yml` contains `postgres:16-alpine` exactly once.
    - `compose.yml` contains NO `redis`, `kafka`, `clickhouse`, `timescale`, `influxdb` substrings.
    - File `Dockerfile.web` and `Dockerfile.worker` exist with `FROM node:22-alpine`.
    - File `.env.example` contains all of: `DATABASE_URL`, `LINEAR_CLIENT_ID`, `LINEAR_CLIENT_SECRET`, `LINEAR_WEBHOOK_SECRET`, `WORKSPACE_ID`, `IDENTITY_CONFIDENCE_THRESHOLD`, `LOG_LEVEL`, `TELEMETRY_OPT_IN`.
    - File `scripts/check-no-req-body.sh` is executable (`-x`) and exits 0 on the empty bootstrap source.
    - File `scripts/smoke-compose.sh` is executable.
    - File `.github/workflows/ci.yml` invokes `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm check:no-req-body`.
    - File `README.md` contains the literal strings "PgBouncer", "pgbouncer=true", "reverse-proxy", "Linear Business or Enterprise".
    - `docker compose config` exits 0 (compose file is valid YAML and resolves all references).
  </acceptance_criteria>
  <done>
    docker-compose stack defined per D-03; CI workflow guards the OBS-04 grep rule; README documents reverse-proxy and PgBouncer per DEPLOY-05/06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → web container | Browser HTTP traffic to Next.js dashboard |
| Public internet → server container | Linear webhook deliveries (HMAC-protected); CLI/SDK API key traffic |
| web container → server container | Server-to-server bearer-authenticated query API calls |
| server/worker → postgres | Internal Docker network; password-protected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.example` shipped with real credentials | mitigate | `.env.example` contains only blank values + dev-only defaults; `.env` is in `.gitignore`; CI does not run with production secrets. |
| T-01-02 | Tampering | Postgres exposed on default port without password | mitigate | `compose.yml` requires `POSTGRES_PASSWORD` env var; default is `agentwatch_dev_password` for local-only convenience; README notes production must override. |
| T-01-03 | Denial of Service | docker-compose port binding to 0.0.0.0 by default | accept | Self-hosted single-tenant; user is responsible for firewall/reverse-proxy. README recommends nginx/Caddy in front (DEPLOY-05). |
| T-01-04 | Information Disclosure | CI logs leak env values | mitigate | Smoke test uses synthetic strings (`smoke-client`, etc.); no production secrets are referenced in CI scripts. |
| T-01-05 | Information Disclosure | OBS-04 grep guard could be bypassed by future code | mitigate | CI runs `bash scripts/check-no-req-body.sh` on every push; failure breaks the build (Pitfall 12). |
</threat_model>

<verification>
- `pnpm install`, `pnpm typecheck`, `pnpm lint`, `pnpm test` all green.
- `docker compose config` exits 0.
- `bash scripts/check-no-req-body.sh` exits 0 on empty source.
- `.github/workflows/ci.yml` passes on the bootstrap commit.
</verification>

<success_criteria>
Workspace boots, lints, types, and tests cleanly. Compose stack reaches placeholder dashboard at :3000. CI guard for OBS-04 is wired and green. README documents the two production-relevant deployment notes (reverse-proxy auth, PgBouncer transaction-mode).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` describing: workspace structure, env-var contract, compose layout, CI gates established, and any deviations from the plan.
</output>
